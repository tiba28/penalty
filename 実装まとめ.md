# 実装まとめ（ペナルティ帳）

最終更新: 2026-07-27
ステータス: **MVP＋AI＋タイムラインまで完成し、Vercel で一般公開済み** 🎉

---

## 1. これは何か

仲間内（サークル・チーム・友人グループ）で、遅刻などの“ルール違反”に点数（ペナルティ）を
つけて、ランキングと罰で盛り上がる Web アプリ。
特徴は **「みんなの合意で成り立つ、民主的なペナルティ帳」** ——
ルールは全員の承認で成立し、加点は本人が認めて確定する公平さが売り。

- リポジトリ: https://github.com/tiba28/penalty
- 公開: Vercel（`git push` で自動デプロイ。配布は Settings→Domains の安定ドメインを使用）

---

## 2. 技術構成

| レイヤー | 採用 |
|---|---|
| フロント | Next.js 16 / React 19（App Router・TypeScript・Tailwind CSS v4） |
| DB・バックエンド | Supabase（Postgres）。全テーブル RLS 有効・anon 全拒否 |
| アクセス方式 | ブラウザは直接DBを叩かず、Next.js サーバー（Route Handler）経由。`service_role` はサーバー専用 |
| 認証 | 独自メンバー認証（bcrypt パスワード＋jose 署名の httpOnly セッションCookie） |
| AI | Google Gemini（無料枠・`gemini-2.5-flash`） |
| ホスティング | Vercel（無料枠） |

---

## 3. 実装した機能

### 基本フロー
- **F1 グループ作成**: 名前・メンバー（最大10人）・集計期間（累計/月次/単発）を決めて招待URL発行
- **F2 参加・ログイン**: 招待URLから名前を選び、初回パスワード設定→以降ログイン
- **初回オンボーディング**: 招待された人に、アプリ説明と使い方を**スライドで初回のみ**表示（スワイプ対応・localStorage記録）

### コア機能
- **F3 ルール**: 誰でも提案 →「本人以外の全員が48時間以内に承認」で成立（1人却下/期限切れで却下）
- **F4 加点**: 誰かに加点を申請（申請者≠本人）→ **本人が48時間以内に認めたら確定**
- **F5 ランキング＋履歴**: 確定加点の合計でワーストランキング（同点は同順位）。加点履歴も表示
- **F7 罰**: 個別しきい値（例 10点で腕立て50回）＋周期（例 5点ごと）。**追加・削除は誰でも提案でき、全員承認で反映**。しきい値到達で「発動中の罰」を表示（表示のみ・点数はリセットしない）

### AI・記録
- **AIに相談**: 対象を選び出来事を入力 → AIが世間の常識と成立済みルールを総合して
  **ペナルティ可否・妥当な点数・理由**を判定。ペナルティなら**自動で加点申請**（確定は本人承認）
- **タイムライン**: AI相談・ルール成立/却下・加点申請・加点確定/却下を時系列で記録。ホーム下部に表示

### 横断
- **認証ガード**: 未ログインで保護ページを開くとログイン画面へ誘導
- **UI**: インディゴ〜バイオレットのグラデ基調。ヒーローヘッダー、メダル順位、カード型ナビ
- **デバッグ表示**: 右上に現在ログイン中のメンバー名（複数ログイン検証用）

---

## 4. データモデル（SupBase / 実DDLは db/ 参照）

- `groups` … グループ（集計期間・招待コード）
- `members` … メンバー（名前・パスワードハッシュ・作成者フラグ）※10人上限
- `rules` / `rule_votes` … ルールと承認投票
- `penalties` … 加点（rule由来 と AI由来 source='ai' の両対応）
- `punishments` / `punishment_votes` … 罰と承認投票（追加・削除の提案）
- `events` … タイムライン（活動ログ）
- ビュー: `member_scores`（ランキング集計）, `penalty_log`（履歴表示）

マイグレーション:
- `db/0001_init.sql` … 初期スキーマ＋RLS
- `db/0002_punishment_approval.sql` … 罰の承認フロー化
- `db/0003_ai_and_timeline.sql` … AI由来の加点＋タイムライン

---

## 5. セキュリティの要点

- 全テーブル **RLS 有効・ポリシー無し** → 公開される anon 鍵ではDBに一切アクセス不可
- 実処理はサーバー側の `service_role` 経由（鍵はフロントに出さない・環境変数で管理）
- パスワードは bcrypt でハッシュ化、セッションは署名付き httpOnly Cookie
- Vercel の環境変数に 5 つを登録（URL・anon・service_role・SESSION_SECRET・GEMINI_API_KEY）

---

## 6. ドキュメント

- [要求定義書.md](要求定義書.md) … 仕様
- [ロードマップ.md](ロードマップ.md) … 今後の拡張案（最優先=通知）
- 開発手順・テスト手順はローカル専用（`*.local.md`、gitには上げない）

---

## 7. 次にやるなら（公開後の候補）

1. **通知**（承認待ちに気づける）… 承認ベースの本アプリと相性が最高
2. **加点の取消・異議申立て**
3. **締切リマインド**

詳細は [ロードマップ.md](ロードマップ.md) を参照。ほぼ全て「追加のみ＝安全に後付け可」。
