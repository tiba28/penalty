# Supabase 接続情報（Project Settings → API から取得）
# 実値は .env.local に記載する（.env.local は git 管理外）
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
